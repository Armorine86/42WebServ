STEP 1.  Parse the config file
	|->	Tokenize every line and pertinent information into vectors
	|->	Record informations into corresponding server fields

**STRUCTURE**

Idea 1):

Server with a single socket class (no Vector):
-> Count how many server block in the config file
--> Allocate Servers on the Heap each containing a socket Class with
	the relevent server info
---> Server class will take ConfigParser as constructor parameter
 |-> Or ConfigParser server_info	

Idea 2):

Server with a vector of sockets:
